"""Auto-assembly in the locked HP montage style (Phase 3).

``style.py`` holds the reusable styling primitives (serif text beats, logo
watermark, outro append) and the pure planning helpers (layout rotation, top-shot
selection). The Dropbox-driven orchestrator that turns scored shots into a
finished review clip lives in ``automation/video_pipeline.py`` (``auto_montage``).
"""
